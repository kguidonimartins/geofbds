% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{fbds_read}
\alias{fbds_read}
\title{Ler dados espaciais de uma camada}
\usage{
fbds_read(x, layer, type = NULL, crs = NULL)
}
\arguments{
\item{x}{Um manifesto, identificadores aceitos por \code{\link[=fbds_resolve]{fbds_resolve()}},
ou um diretorio.}

\item{layer}{Uma camada de \code{\link[=fbds_layers]{fbds_layers()}}.}

\item{type}{Tipo do conjunto de shapefile (ex. \code{"APP"},
\code{"MASSAS_DAGUA"}). Obrigatorio quando a camada tem mais de um tipo
disponivel entre os arquivos encontrados.}

\item{crs}{Sistema de referencia para o qual harmonizar (via
\code{\link[sf:st_transform]{sf::st_transform()}}) antes de empilhar os municipios. \code{NULL}
(padrao) nao transforma — se os municipios tiverem CRS diferentes,
o empilhamento falha com o erro do proprio sf.}
}
\value{
Um objeto \code{sf}, com uma linha por feicao e os municipios
empilhados; inclui as colunas \code{geocode} e \code{uf}.
}
\description{
Aceita um manifesto (de \code{\link[=fbds_fetch]{fbds_fetch()}} e afins), geocodigos/nomes
resolviveis por \code{\link[=fbds_resolve]{fbds_resolve()}}, ou um diretorio. Para geocodigos ou
diretorio, assume o layout padrao de \code{\link[=fbds_plan]{fbds_plan()}}
(\code{{uf}/{geocode}/{layer}/arquivo}); um download com \code{path_pattern}
customizado so e localizavel passando o manifesto.
}
\details{
Uma camada pode ter mais de um conjunto de shapefile (ex.
\code{HIDROGRAFIA} tem \code{MASSAS_DAGUA}, \code{NASCENTES}, \code{RIOS_DUPLOS} e
\code{RIOS_SIMPLES}) com geometrias distintas, que nao fazem sentido
empilhadas juntas — por isso \code{type} e obrigatorio sempre que houver
mais de um. Um conjunto de shapefile incompleto (falta \code{.shp}, \code{.shx},
\code{.dbf} ou \code{.prj}) e ignorado com aviso, nunca lido.
}
\examples{
\dontrun{
m <- fbds_download_municipality("1100031", layers = "app")
fbds_read(m, layer = "app", type = "APP")
}
}
