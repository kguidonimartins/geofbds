% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{fbds_palette}
\alias{fbds_palette}
\title{Cores padronizadas para classes tematicas dos dados da FBDS}
\usage{
fbds_palette(field, values = NULL)
}
\arguments{
\item{field}{Nome do campo tematico (ex. \code{"CLASSE_USO"}). Aceita qualquer
nome de coluna; campos sem cores conhecidas usam so a paleta de reserva.}

\item{values}{Vetor com as classes a colorir (tipicamente uma coluna de um
objeto \code{sf}, ex. \code{dados$CLASSE_USO}). \code{NULL} (padrao) devolve todas as
cores conhecidas de \code{field}, ou um vetor vazio se \code{field} nao tiver
cores conhecidas.}
}
\value{
Vetor de cores nomeado (codigos hex), um elemento por classe
distinta.
}
\description{
Devolve um vetor de cores nomeado para colorir mapas por classe, sem
redefinir a paleta a cada script. Cobre os campos categoricos mais comuns
dos shapefiles da FBDS: \code{"CLASSE_USO"} (uso e cobertura do solo, em
\code{uso/USO} e \code{app/APP_USO}), \code{"HIDRO"} (feicao hidrografica que originou a
APP, em \code{app/APP} e nos conjuntos de \code{hidrografia}), e \code{"NATUREZA"}/\code{"RIO"}
(ambos de \code{hidrografia/MASSAS_DAGUA}). Classes conhecidas destes campos
recebem sempre a mesma cor; classes desconhecidas (de outro campo, ou uma
categoria nova) recebem uma cor de reserva estavel, tirada de uma paleta
qualitativa (ColorBrewer "Dark2"), na ordem em que aparecem em \code{values}.
}
\details{
As cores de \code{"CLASSE_USO"} seguem o MapBiomas Colecao 11, para
consistencia visual com a plataforma mais usada para mapas de uso e
cobertura do solo no Brasil (correspondencia classe a classe documentada
em \code{R/palette.R}).
}
\examples{
fbds_palette("CLASSE_USO")
fbds_palette("HIDRO")

\dontrun{
uso <- fbds_get("serra da saudade", layer = "uso", type = "USO")
cores <- fbds_palette("CLASSE_USO", uso$CLASSE_USO)
plot(uso["CLASSE_USO"], col = cores[uso$CLASSE_USO])
}
}
