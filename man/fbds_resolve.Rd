% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{fbds_resolve}
\alias{fbds_resolve}
\title{Resolver identificadores de municipio em geocodigos canonicos}
\usage{
fbds_resolve(x, uf = NULL, strict = TRUE)
}
\arguments{
\item{x}{Vetor de geocodigos, nomes, UFs ou \code{"Municipio/UF"}.}

\item{uf}{UF usada para desambiguar um nome de municipio informado sem
UF. Ignorada para entradas que ja trazem geocodigo, UF ou
\code{"Municipio/UF"}.}

\item{strict}{Se \code{TRUE} (padrao), um identificador que nao resolve a
nenhum municipio interrompe a execucao com
\code{fbds_unknown_municipality}. Se \code{FALSE}, o identificador e ignorado com
um aviso.}
}
\value{
Vetor nomeado de geocodigos (texto, 7 digitos), com o nome do
municipio do catalogo em cada elemento, sem duplicatas e na ordem de
primeira ocorrencia.
}
\description{
Ponto de entrada universal do pacote: aceita geocodigo (numerico ou
texto), sigla de UF (expande para todos os municipios), nome de municipio
(sem acento, caixa indiferente) e a forma \code{"Municipio/UF"}. Ambiguidade
(o mesmo nome em mais de uma UF) nunca e resolvida em silencio: gera um
erro classificado (\code{fbds_ambiguous_municipality}) listando os candidatos.
}
\examples{
fbds_resolve("1100031")
fbds_resolve("cabixi")
fbds_resolve("Cabixi/RO")
fbds_resolve("RO")
}
