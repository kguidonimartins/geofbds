% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fbds_fetch}
\alias{fbds_fetch}
\title{Executar um plano de download}
\usage{
fbds_fetch(
  plan,
  workers = getOption("geofbds.workers", 4L),
  retries = getOption("geofbds.retries", 3L),
  timeout = getOption("geofbds.timeout", 300),
  progress = getOption("geofbds.progress", TRUE),
  dry_run = FALSE
)
}
\arguments{
\item{plan}{Um objeto \verb{<fbds_plan>} de \code{\link[=fbds_plan]{fbds_plan()}}.}

\item{workers}{Tamanho do bloco de download concorrente.}

\item{retries}{Numero de novas tentativas para arquivos que falharem.}

\item{timeout}{Tempo maximo (segundos) por arquivo, por tentativa.}

\item{progress}{Se \code{TRUE} (padrao), informa o andamento via \code{cli}.}

\item{dry_run}{Se \code{TRUE}, nao baixa nada: devolve o manifesto que
resultaria (\code{status} \code{"cached"} ou \code{"skipped"}) sem tocar a rede.}
}
\value{
Um manifesto (tibble), tambem gravado em
\verb{\{dest_dir\}/_manifests/\{run_id\}.csv}.
}
\description{
Sobre \code{\link[curl:multi_download]{curl::multi_download()}} com \code{resume = TRUE}: substitui o laco
sequencial e a logica manual de \code{.part} do script original
(SPECS.md §2.2-14). \code{curl::multi_download()} nao expoe um parametro de
concorrencia real, entao \code{workers} particiona as linhas em blocos
desse tamanho, cada um resolvido em uma chamada. Apos cada rodada, as
linhas que falharam sao re-tentadas com backoff, ate \code{retries} vezes.
}
\examples{
\dontrun{
plano <- fbds_plan("1100031", layers = "app")
fbds_fetch(plano)
}
}
