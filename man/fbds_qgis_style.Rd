% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qgis.R
\name{fbds_qgis_style}
\alias{fbds_qgis_style}
\title{Copiar os estilos do QGIS para o projeto do usuario}
\usage{
fbds_qgis_style(field = NULL, dest_dir = ".", overwrite = FALSE)
}
\arguments{
\item{field}{Nome de um ou mais campos com estilo disponivel (ex.
\code{"CLASSE_USO"}). \code{NULL} (padrao) copia todos. Ver
\code{\link[=fbds_qgis_style_fields]{fbds_qgis_style_fields()}} para os disponiveis.}

\item{dest_dir}{Diretorio de destino. Padrao \code{"."} (diretorio de trabalho
atual). Criado se nao existir.}

\item{overwrite}{Sobrescrever arquivo(s) ja existentes em \code{dest_dir}?
Padrao \code{FALSE}: para sem copiar nada se algum arquivo de destino ja
existir.}
}
\value{
Vetor com os caminhos dos arquivos copiados, invisivelmente.
}
\description{
Os arquivos \code{.qml} (estilos categorizados do QGIS, com as mesmas cores de
\code{\link[=fbds_palette]{fbds_palette()}}) ficam dentro do pacote instalado -- em
\code{system.file("qgis", package = "geofbds")} --, um lugar que quem instala o
pacote via CRAN, \code{remotes::install_github()} ou similar normalmente nunca
abre. Sem copia-los para fora, o QGIS nao enxerga esses arquivos: esta
funcao faz essa copia, por padrao para o diretorio de trabalho atual
(tipicamente a raiz do projeto do usuario).
}
\examples{
dest <- tempfile("qgis-styles-")
fbds_qgis_style(dest_dir = dest)
list.files(dest)
unlink(dest, recursive = TRUE)
}
