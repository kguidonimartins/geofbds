% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{fbds_download_municipality}
\alias{fbds_download_municipality}
\title{Baixar dados de um ou mais municipios}
\usage{
fbds_download_municipality(
  x,
  layers = NULL,
  dest_dir = fbds_cache_dir(),
  recursive = FALSE,
  skip_existing = TRUE,
  path_pattern = "{uf}/{geocode}/{layer}/{file}",
  workers = getOption("geofbds.workers", 4L),
  retries = getOption("geofbds.retries", 3L),
  timeout = getOption("geofbds.timeout", 300),
  progress = getOption("geofbds.progress", TRUE),
  dry_run = FALSE
)
}
\arguments{
\item{x}{Identificadores aceitos por \code{\link[=fbds_resolve]{fbds_resolve()}}.}

\item{layers}{Camadas de \code{\link[=fbds_layers]{fbds_layers()}}. \code{NULL} (padrao) usa todas.}

\item{dest_dir}{Diretorio de destino. Padrao \code{\link[=fbds_cache_dir]{fbds_cache_dir()}}.}

\item{recursive}{Se \code{TRUE}, inclui subdiretorios (ex. \code{USO/MAPAS}).}

\item{skip_existing}{Se \code{TRUE} (padrao), um arquivo ja presente em
\code{dest_dir} com tamanho a ate 1 KB do esperado e marcado para pular
(sem nova requisicao). A listagem do portal so informa tamanho em
KB, daí a tolerancia; a palavra final sobre integridade e de
\code{\link[=fbds_fetch]{fbds_fetch()}}, que confere o arquivo de verdade apos a transferencia.}

\item{path_pattern}{Padrao do caminho local, com placeholders \code{{uf}},
\code{{geocode}}, \code{{layer}}, \code{{file}}. Quando um arquivo vem de um
subdiretorio remoto (\code{recursive = TRUE}), \code{{file}} ja inclui esse
subdiretorio como prefixo.}

\item{workers}{Tamanho do bloco de download concorrente.}

\item{retries}{Numero de novas tentativas para arquivos que falharem.}

\item{timeout}{Tempo maximo (segundos) por arquivo, por tentativa.}

\item{progress}{Se \code{TRUE} (padrao), informa o andamento via \code{cli}.}

\item{dry_run}{Se \code{TRUE}, nao baixa nada: devolve o manifesto que
resultaria (\code{status} \code{"cached"} ou \code{"skipped"}) sem tocar a rede.}
}
\value{
O manifesto (tibble) de \code{\link[=fbds_fetch]{fbds_fetch()}}.
}
\description{
Involucro fino de \code{fbds_plan() |> fbds_fetch()}.
}
\examples{
\dontrun{
fbds_download_municipality(c("1100031", "1100049"), layers = "app")
fbds_download_municipality("Cabixi/RO")
}
}
